include: package:flutter_lints/flutter.yaml

linter:
  rules:
    - prefer_double_quotes

analyzer:
  language:
      strict-casts: true
      strict-raw-types: true
  plugins:
    - custom_lint
